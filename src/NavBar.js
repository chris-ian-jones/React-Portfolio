import React from 'react';
import styled from 'styled-components'

const StyledPlaceholder = styled.p`
    color: red;
`

function NavBar() {

    return(    
        <div>
            <StyledPlaceholder>nav placeholder</StyledPlaceholder>
        </div>
    )
}

export default NavBar
